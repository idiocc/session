<types namespace="_idio">
  <type interface name="KoaSession" description="The session model.">
    <prop type="boolean" name="isNew">
      Returns true if the session is new.
    </prop>
    <prop type="boolean" name="populated">
      Populated flag, which is just a boolean alias of `.length`.
    </prop>
    <prop type="number" name="maxAge">
      Get/set cookie's maxAge.
    </prop>
    <prop type="function(): void" name="save">
      Save this session no matter whether it is populated.
    </prop>
    <prop type="function(): Promise" name="manuallyCommit">
      Session headers are auto committed by default. Use this if autoCommit is set to false.
    </prop>
  </type>
</types>